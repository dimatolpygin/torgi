# bron-bot — инструкции проекта

Бот автоматической брони торгового места на `gorod.it-minsk.by` (Минский Комаровский
рынок, овощи). Подаёт заявку в 00:00 по Минску быстрее конкурентов, проверяет результат
по списку броней в ЛК, шлёт уведомление в Telegram. Два аккаунта (клиент + муж).

## ⚠ Текущая ситуация (20.07.2026) — ЗАМОРОЖЕНО по решению клиентки

**Бот стабилен и делает ядро на 100%: 2/2 места каждую ночь на обоих аккаунтах, без
сбоев/дублей.** Прод на master (BY `45.128.205.148`, переезд 01.08.2026), `DRY_RUN=false`, `BOOKINGS_PER_ACCOUNT=2`,
`SUBMIT_GAP=0` (обе в гонку), `MULTICONNECT_K=1`. Последний закрытый этап — **23** (развязка
выстрелов, tag `stage-23-done`): убран баг «+8 с у 2-й заявки» (обе заявки шли на одном сокете
последовательными `await`; теперь при gap=0 каждое место на своём тёплом сокете, залп параллельно
`submitBookingsParallel`). Добавлена честная телеметрия `maxResponseMs` (время ответа сервера).

**ГЛАВНЫЙ ВЫВОД Вехи 2 (доказан живыми ночами 14–19.07):** миллисекундный send-тайминг — **НЕ
рычаг ранга**. Наш выстрел идеален (drift +0 мс, отправка +0..+23 мс, RTT 3–5 мс), но место в
утреннем списке админа — **7–14**, тогда как в начале июля (тот же бот, меньше конкурентов) было
1–4. Причина: `create_zajav` в 00:00 обрабатывается сервером **~3–7 с** (глобальная очередь,
`min≈max` по сокетам → сервер не сериализует наши сокеты, все стоят в общей очереди). Ранг
решается порядком КОММИТА внутри этого окна, а не тем, кто отправил первым. Дело в выросшей
конкуренции (чужие боты), не в нашем коде.

**Единственный непроверенный рычаг ранга — плотность соединений (мультиконнект K≫1, возможно ×
несколько IP).** Ему НЕ нужен более мощный сервер (сокеты дёшевы, залп — I/O; текущего 1CPU/2GB
хватает на 100+ сокетов); потолок — лимит POST на один IP (риск `fail2ban`/блокировки боевого IP)
и гонка вооружений. `send-ahead` (этап 20) — дёшев, но эффект условный (сервер может вернуть
`no_date`) и мелкий против 3–7 с. Систематический «+7 мс мужа» — артефакт последовательной
диспетчеризации аккаунтов в одном event-loop (муж 2-й в массиве), на ранг не влияет (тонет в очереди).

**Депориоритет:** этапы 20 (упреждение) и 24 (хостинг ближе) — их мс-выигрыш ничтожен против 3–7 с.

**Решение клиентки (18.07):** «оставим всё как есть», просит ссылку на оплату → Веха 2 приостановлена.
**НЕ катить мультиконнект/изменения без явного слова.** Если разморозят — готовый путь: 1 ночь
`MULTICONNECT_K=12` (нужна доработка этапа 22: залп на ОБА места без регрессии этапа 23) + смотреть
ранг и признаки защиты (`429/503`/обрывы). Подробности — в `docs/ROADMAP.md` (блок «⏱ Серверная
латентность») и `docs/STATUS.md`.

### 🏁 Гонка ботов — фактическая картина (июль 2026)

Место в утреннем списке админа рынка (данные от клиентки; дата брони = ночь подачи + 7):

| Дата рынка | Ночь подачи | Версия бота | Ответ сервера | Место жена / муж |
|---|---|---|---|---|
| 16.07 | 09.07 | старая | — | **1 / 4** |
| 17.07 | 10.07 | старая | — | **1 / 4** |
| 18.07 | 11.07 | старая | — | **1 / 4** |
| 19.07 | 12.07 | старая (баг +8 с) | — | **11 / 15** |
| 21.07 | 14.07 | этап 23 | 4,5 с | 11 / 12 |
| 22.07 | 15.07 | этап 23 | 3,9–6,1 с | 7 / 12 |
| 23.07 | 16.07 | этап 23 | **3,0 с** | 10 / 7 и 9 |
| 24.07 | 17.07 | этап 23 | **6,8 с** | 13–14 / 11–12 |

**Как читать:** в начале июля тот же бот брал 1–4 места; с середины июля при ИДЕАЛЬНОМ исполнении
(+0 мс) — 7–14. Код не деградировал (наоборот, этап 23 убрал самоинфликт) — выросло число чужих
ботов. Слабый сигнал: чем быстрее сервер ответил в ту ночь, тем лучше ранг (16.07: 3,0 с → 7–10;
17.07: 6,8 с → 11–14), но скоростью чужого сервера мы не управляем.

**Что, вероятно, делают конкуренты** (вывод по нашим данным, их код мы не видели): держат
ДЕСЯТКИ тёплых соединений (возможно × несколько аккаунтов/IP) и в 00:00:00.000 бьют залпом своей
РЕАЛЬНОЙ заявкой → занимают первые слоты очереди раньше, чем до них доходят наши 2 сокета.
Сервер под нагрузкой заявки не отбрасывает (мы всегда получали `201`, ни одного `503` даже на
6,8 с) — он ставит всех в общую очередь и коммитит по порядку принятия.

**Опровергнутая гипотеза:** «конкуренты флудят мусором, чтобы поймать окно, когда сервер
освободится» — не работает: пауз в очереди нет, а подача «после того как схлынет» = попадание в
ХВОСТ (места 11–15). Побеждает тот, кто плотнее в НАЧАЛЕ очереди, а не тот, кто ждал.

**Рычаги обхода — только легитимные (свои реальные заявки), по цене/риску:**
1. `SEND_AHEAD_MS` (этап 20) — 0 денег, код готов; эффект условный (сервер может дать `no_date`,
   есть безопасный фолбэк) и мелкий против 3–7 с. Требует калибровки живыми ночами.
2. Плоский залп (убрать «+7 мс мужа») — 0 денег, косметика, ранг не двинет.
3. **Мультиконнект `K≫1`** — единственное, что реально двигает ранг; риск `fail2ban` на боевом IP.
4. Несколько дешёвых минских VPS ради РАЗНЫХ IP — снимает потолок п.3 (каждый VPS может быть
   1CPU/1GB; мощный сервер не нужен НИКОМУ из этих пунктов).

**Чего не делаем:** флуд фейковыми заявками для замедления конкурентов — это DoS против
госсервера, отказ; вдобавок бесполезно (тормозит нашу же заявку в той же очереди).

**Что говорить клиентке:** ядро (2/2 места каждую ночь) бот выполняет на 100%. РАНГ в списке
гарантировать нельзя — он решается серверной очередью и числом конкурентов, а не миллисекундами.

## Контекст проекта (читать для полного понимания сути)

- **Переписка с клиентом / ТЗ:** `C:\Users\GigaChat\Downloads\ЗАКАЗЫ\bron\переписка_с_тз.txt`
  — полная суть: клиент регистрируется **каждый день** в 00:00 по Минску на торговое
  место (новая дата открывается на неделю вперёд), хочет обгонять конкурентов с ботами
  (решает близость сервера к сайту → боевой сервер белорусский). По понедельникам рынок
  не работает — дату на понедельник бронировать нельзя. Лимит 2 места/день: бот подаёт 1,
  клиент вручную 2-ю (подстраховка). Тариф «Стандарт» = с Telegram-уведомлениями.
- **Доступы (сервер, GitHub):** `C:\Users\GigaChat\Downloads\ЗАКАЗЫ\bron\доступы.txt`
  — тест-сервер 163.5.153.147 (root), публичный репо https://github.com/dimatolpygin/torgi.
- Деплой-ключ и серверный `.env` — локально в `.secrets/` (gitignored). Прод-инфа — в
  разделе «Прод» в `docs/STATUS.md`.

## Старт сессии (обязательный ритуал okplan)

1. Прочитать `docs/STATUS.md` — текущий этап, активная работа, блокеры.
2. `git tag -l "stage-*"` и сверить с таблицей в STATUS. Тег без записи = STATUS устарел.
3. `git log --oneline -10` — что менялось последним.
4. Открыть `docs/ROADMAP.md` на текущем этапе — открытые чекбоксы.

Только после этого браться за задачу.

## Стек

- Node.js 20+ (ESM), без TypeScript.
- HTTP: `undici` (keep-alive, контроль заголовков, скорость выстрела).
- Telegram: `telegraf`. Логи: `pino` + `pino-pretty` (русские сообщения, дата/время).
- Postgres (`pg`) — история попыток, аккаунты. Redis (`ioredis`) — сессии, флаги.
- Тайминг: `luxon` (таймзона Europe/Minsk).
- Docker + docker-compose. CI/CD — GitHub Actions (через okdeploy).

## Ключевая механика сайта (выяснено разбором)

- Логин: POST `/rinki/minsk/login/`, поля `n_login`, `n_pass`. Сессия — кука `gorodid`.
- Капчи/CSRF/WAF нет. Сервер — Apache, госЦОД Мингорисполкома (IP 87.252.228.216).
- При выборе рынка — AJAX (логика в `js/rinki/rinki.reg.js`):
  - `type_mest/` POST `{RINOK}` → `{KVO_TORG_MASH, KVO_TORG_MEST}` (1=машино-место, 2=торговый ряд).
    На Комаровском дефолт = «Торговый ряд» (value=2) — его и не трогаем.
  - `limit_mest/` POST `{RINOK, TYPE_PERSON}` → `{REZERV_LIMIT}` (на Комаровском = 2).
  - `calend/` POST `{f_reg:1, ID_RINKA, TYPE_MEST, DATA_YEAR, DATA_MONTH, DATA_DAY}` → `{answer: html}`.
    Доступный день = кликабельный `<a class="day" day="N">`.
- **Реальная подача — НЕ на reg/fiz, а POST `create_zajav/`** с полями:
  `PERSN` (JSON {n_persn,fam,name,otc}), `rinok`, `type_mesta`, `arr_date` (JSON {0:"день"}),
  `month`, `year`, `t_contakt`, `n_mail`, `pass`, `pass_sub`, `ARR_ASSORT` (JSON {0:"2"}=овощи),
  `type_person=fiz`, `is_login=1`, `f_sogl`. Ответ: code 201 = принято (залогинен),
  200 = принято+создан ЛК, 500 = ошибки валидации, 501 = пароль неверный.
- **Диагностика (этап 3):** для залогиненного (`is_login=1`) сервер требует ТОЛЬКО валидную
  `arr_date` + ассортимент. Поля персоны/пароля можно слать пустыми — сервер берёт из сессии.
  Запрос с пустой датой даёт `code 500, {arr_date:"no_date"}` и заявку НЕ создаёт (безопасно).
  В ответе `""` у поля = ошибки нет, иначе `empty`/`no_date`/`no_assort`/`length`.
- Подтверждение успеха — таблица «Для Вас забронированы следующие места» в `account/`.
- В 00:00 открывается одна новая дата (неделя вперёд). По понедельникам регистрации нет.
- Лимит 2 заявки/сутки: бот подаёт 1-ю, клиент вручную 2-ю (подстраховка).

## Правила работы

- После каждого изменения кода — git commit + push в ветку `dev`.
- В `master` мержить только по явному указанию (там автодеплой через Actions).
- Каждый этап закрывается только после живой проверки (UAT) + git-тег `stage-N-done`.
- Секреты (логины, токены) — только в `.env` / GitHub Secrets, никогда в репозитории.
- **Ветка отката `save` (`3c8954b`)** — зафиксированный рабочий вариант перед Вехой 2
  (оптимизация скорости): стабильная подача, баг дублей исправлен, разрыв 2-й заявки 1 с.
  При регрессии тайминга сверяться/откатываться на неё. Не двигать без явного указания.
