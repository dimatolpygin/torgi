name: Deploy

# Боевой сервер с 01.08.2026 — 45.128.205.148 (задан в секрете SERVER_HOST).
# Прежний 155.212.244.79 погашен, каталог там переименован в /opt/bron-bot.old-155.

on:
  push:
    branches: [master]
    # Docs-/markdown-коммиты не пересобирают бота на проде (иначе клиентке каждый
    # раз летит «бот запущен»). Деплой только при изменениях кода.
    paths-ignore:
      - 'docs/**'
      - '**.md'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script: |
            set -e
            cd ${{ secrets.INSTALL_DIR || '/opt/bron-bot' }}
            git fetch --all
            git reset --hard origin/master
            docker compose up -d --build
            docker image prune -f
