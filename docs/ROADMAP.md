# Дорожная карта реализации — bron-bot

Бот автоматической брони торгового места на `gorod.it-minsk.by` (Минский Комаровский
рынок, овощи). Подаёт заявку в 00:00 по Минску быстрее конкурентов, проверяет результат
по списку броней в ЛК и шлёт уведомление в Telegram.

11 этапов от пустого репозитория до боевой готовности. Каждый этап заканчивается
**демонстрируемым результатом**.

> **Где смотреть прогресс**: текущий статус всех этапов — в [`STATUS.md`](STATUS.md).
> Закрытие этапа сопровождается git-тегом `stage-N-done`.

> **Важное ограничение тест-периода**: пока брони на сайте недоступны (открываются ночью
> в 00:00, разбираются за минуты), реальную успешную подачу проверить нельзя. Поэтому этапы
> 2–4 верифицируются на «пустом» состоянии (бот корректно видит «дат нет» и формирует
> запрос), а финальная боевая проверка реальной подачи — отдельный пост-этап на белорусском
> сервере в боевую ночь.

| #  | Этап | Главный артефакт | Зависит от |
|----|------|------------------|------------|
| 0  | Каркас репозитория | `docker compose up` поднимает app+postgres+redis, лог пишется | — |
| 1  | Авторизация на сайте | Бот логинится под аккаунтом, в логе «вошли как <ФИО>» | 0 |
| 2  | Чтение состояния рынка | Бот сообщает доступные даты/типы мест по Комаровскому | 1 |
| 3  | Формирование заявки (dry-run) | Готовый POST собран корректно, виден в логе | 2 |
| 4  | Верификация через ЛК | Бот выводит актуальный список броней из кабинета | 1 |
| 5  | Точный планировщик и тайминг | Выстрел в заданную секунду с точностью ±несколько мс, пропуск понедельника | 3 |
| 6  | Мультиаккаунт | Параллельно ведутся 2 аккаунта (она + муж) | 5 |
| 7  | Telegram-уведомления | Результат прогона приходит в Telegram | 4,6 |
| 8  | Хранилище и история | Каждая попытка пишется в Postgres, сессии — в Redis | 6 |
| 9  | Docker-деплой + CI/CD | Пуш в master → автодеплой на тест-сервер | 8 |
| 10 | E2E тест-прогон на сервере | Имитация полуночи на сервере, уведомление пришло | 9 |

---

## Этап 0 — Каркас репозитория

**Цель**: пустой, но рабочий проект со всей инфраструктурой для следующих этапов.

**Что делаем**:
- `package.json`, зависимости (undici, telegraf, pino, pg, ioredis, luxon, dotenv).
- `docker-compose.yml`: сервисы app, postgres, redis.
- `Dockerfile`, `.env.example`, `.gitignore`.
- Базовый `src/index.js`: подключается к Postgres и Redis, пишет лог через pino, держится живым.
- Логгер по правилам проекта (pino + pino-pretty, русские сообщения).

**Критерии приёмки** (закрытие → tag `stage-0-done`):
- [x] `docker compose up` поднимает три контейнера без ошибок
- [x] В логе видно успешное подключение к Postgres и Redis
- [x] Лог на русском, читаемый, с датой/временем
- [x] `.env.example` содержит все нужные переменные, секретов в репозитории нет

---

## Этап 1 — Авторизация на сайте

**Цель**: бот умеет войти в кабинет и подтвердить, что вход успешен.

**Что делаем**:
- Модуль `auth`: POST на `/rinki/minsk/login/` с `n_login`/`n_pass`.
- Сохранение сессионной куки `gorodid`.
- Проверка входа: парсинг ЛК на «пользователя <логин>» / ФИО.

**Критерии приёмки** (закрытие → tag `stage-1-done`):
- [x] Бот логинится под тестовым аккаунтом, в логе «вошли как <ФИО>»
- [x] При неверном пароле бот понимает, что вход не удался, и пишет это в лог
- [x] Сессионная кука сохраняется и переиспользуется между запросами

---

## Этап 2 — Чтение состояния рынка

**Цель**: бот видит реальное состояние формы по Комаровскому рынку.

**Что делаем**:
- Выбор рынка Комаровский (`rinok=10`).
- Вызовы AJAX `type_mest/`, `limit_mest/`, `calend/`.
- Парсинг: есть ли доступная дата, какой тип места по умолчанию.

**Критерии приёмки** (закрытие → tag `stage-2-done`):
- [x] Бот корректно сообщает «дат нет» в дневное время (пустое состояние)
- [x] Бот извлекает тип места по умолчанию и список дат из ответов AJAX
- [x] Ответы сервера логируются для разбора боевого формата

---

## Этап 3 — Формирование заявки (dry-run)

**Цель**: бот собирает точный запрос на подачу, но пока не отправляет вслепую.

**Что делаем**:
- Сборка POST на `/rinki/minsk/reg/fiz/`: `reg=1`, `rinok=10`, `type_mesta`, `arr_date`,
  `assort_arr[]=2` (овощи), служебные поля.
- Режим dry-run: запрос формируется и логируется, реальная отправка под флагом.

**Критерии приёмки** (закрытие → tag `stage-3-done`):
- [x] В логе виден полностью сформированный запрос, готовый к отправке
- [x] Ассортимент жёстко = овощи, рынок = Комаровский, дата берётся из состояния
- [x] Флаг `DRY_RUN` управляет реальной отправкой

**Доп. проверка (диагностика сервера):** запрос с пустой датой → `code 500, {arr_date:"no_date"}`,
поля персоны не помечены ошибкой → для залогиненного нужна только дата + ассортимент.

---

## Этап 4 — Верификация через ЛК

**Цель**: бот подтверждает успех не по ответу формы, а по факту в списке броней.

**Что делаем**:
- Парсинг таблицы «Для Вас забронированы следующие места» в `account/`.
- Сопоставление: появилась ли запись на нужную дату, Комаровский, овощи.

**Критерии приёмки** (закрытие → tag `stage-4-done`):
- [x] Бот выводит актуальный список броней аккаунта из ЛК
- [x] Бот умеет ответить «есть ли бронь на дату X» по содержимому ЛК

---

## Этап 5 — Точный планировщик и тайминг

**Цель**: бот сам просыпается к полуночи и стреляет с высокой точностью.

**Что делаем**:
- Пробуждение заранее (≈23:58), прогрев сессии и keep-alive соединения.
- Точный выстрел в 00:00:00.000 по `Europe/Minsk` через hrtime-ожидание.
- Пропуск ночей на понедельник (регистрации нет).
- Режим «долбёжки» при отсутствии даты (редкие сбои: места открываются позже).

**Критерии приёмки** (закрытие → tag `stage-5-done`):
- [x] При подставной целевой минуте бот срабатывает в заданную секунду, отклонение в логе ≤ несколько мс
- [x] Ночь на понедельник пропускается
- [x] Если даты нет в 00:00 — бот переходит в режим повторных проверок, а не сдаётся

---

## Этап 6 — Мультиаккаунт

**Цель**: одна система обслуживает два кабинета (она + муж) параллельно.

**Что делаем**:
- Конфигурация списка аккаунтов.
- Параллельная подготовка и подача с изоляцией сессий.

**Критерии приёмки** (закрытие → tag `stage-6-done`):
- [x] Бот ведёт 2 аккаунта одновременно, в логе видно действия по каждому
- [x] Сессии аккаунтов не путаются между собой

---

## Этап 7 — Telegram-уведомления

**Цель**: результат каждой ночи приходит в Telegram.

**Что делаем**:
- Бот на telegraf, регистрация chat_id через `/start`.
- Сообщения: «подано», «подтверждено (место, дата)», «ошибка — подайте вручную».

**Критерии приёмки** (закрытие → tag `stage-7-done`):
- [x] После прогона в Telegram приходит сообщение с результатом по каждому аккаунту
- [x] При неуспехе приходит явное предупреждение подать вручную
- [x] Команда `/status` отдаёт состояние сервера (аптайм, след. подача, режим)
- [x] Алерт о возможной блокировке IP приходит при серии ошибок на подаче

---

## Этап 8 — Хранилище и история

**Цель**: каждая попытка фиксируется, состояние переживает перезапуск.

**Что делаем**:
- Схема Postgres: аккаунты, попытки, результаты, отклонение тайминга.
- Redis: хранение актуальных сессионных кук с TTL, флаги «сегодня подано».

**Критерии приёмки** (закрытие → tag `stage-8-done`):
- [x] После прогона в Postgres появляется запись о попытке с деталями и результатом
- [x] Сессионная кука берётся из Redis при повторном запуске, лишнего логина нет

---

## Этап 9 — Docker-деплой + CI/CD

**Цель**: проект разворачивается на сервере автоматически (через okdeploy).

**Что делаем**:
- Финальный Docker-образ, GitHub Actions деплой на тест-сервер `163.5.153.147`.
- Секреты в GitHub Secrets, не в репозитории.

**Критерии приёмки** (закрытие → tag `stage-9-done`):
- [x] Пуш в master запускает автодеплой, на сервере поднимается актуальная версия
- [x] Бот стабильно работает на сервере в фоне (restart policy)

---

## Этап 10 — E2E тест-прогон на сервере

**Цель**: полный цикл на тест-сервере с имитацией полуночи.

**Что делаем**:
- Прогон с подставной целевой минутой на сервере.
- Проверка всей цепочки: логин → состояние → формирование → верификация → Telegram.

**Критерии приёмки** (закрытие → tag `stage-10-done`):
- [x] На сервере отрабатывает полный цикл (по доступному аккаунту; 2-й добавится с реальными кредами мужа)
- [x] Уведомление приходит в Telegram
- [x] Система готова к переносу на белорусский сервер для боевой ночи

---

## Пост-этап (вне тест-периода) — Боевая верификация

Не входит в тест-фазу: реальная успешная подача в 00:00 на белорусском сервере, когда
брони открыты. Проверяется в режиме подстраховки (бот + ручная подача клиента
параллельно), несколько ночей подряд, до перехода в автономный режим.

---

# Цикл 2 — Проверка и доводка перед боевым запуском

Этапы 0–10 (тест-фаза) закрыты. Этот цикл — финальная доводка под требование клиента
«быстро и надёжно»: ускорить подачу, привести в порядок тексты, перепроверить работу
с сайтом и расписание, подготовить к боевому запуску.

## Этап 11 — Часовой пояс и механизм запуска

**Цель**: убедиться, что бот гарантированно просыпается в 00:00 по Минску каждый день
(кроме ночи на понедельник), и логи читаемы.

**Что делаем**:
- Зафиксировать механизм: внутренний планировщик процесса (не крон), `restart: unless-stopped`.
- Выставить TZ контейнера `Europe/Minsk` (чтобы логи были в минском времени; на расчёт
  расписания не влияет — luxon уже считает в Минске).
- Проверить расписание ближайших запусков и корректность пропуска понедельника против
  реального открытия дат на сайте (дата брони = день старта +7).

**Критерии приёмки** (закрытие → tag `stage-11-done`):
- [x] Подтверждено: планировщик внутренний, переживает рестарт сервера _(UAT на тест-сервере: `docker compose restart app` → контейнер поднялся, планировщик пересчитал «Следующая подача: вс 21.06 00:00» от текущего времени; не крон, состояние не теряется)_
- [x] TZ контейнера = Europe/Minsk, логи в минском времени _(UAT: в контейнере `TZ=Europe/Minsk`, `date`=+03; docker-метка 05:59Z (UTC) vs лог pino [08:59] — минское время. Фикс `SYS:` в pino-pretty + TZ в compose)_
- [x] Расписание совпадает с реальностью сайта (включая пропуск понедельника) _(check-schedule.js: 10 ночей подачи → дата +7, ни одной ночи/брони на понедельник, детерминизм пересчёта)_

## Этап 12 — Оптимизация скорости подачи

**Цель**: в 00:00 уходит минимум запросов — только сама подача.

**Что делаем**:
- В прогрев (≈23:58) перенести: поля формы reg/fiz, тип/лимит места, предвычисление даты.
- В 00:00 — только `create_zajav` (1 запрос). Замерить «от 00:00 до подачи».

**Критерии приёмки** (закрытие → tag `stage-12-done`):
- [x] В 00:00 выполняется один запрос подачи (остальное — в прогреве) _(check-speed.js: быстрый путь — 0 предварительных чтений рынка/формы; живой E2E: прогрев в −25с тянет поля+тип, в 00:00 только create_zajav)_
- [x] Замер «от 00:00 до подачи» ≈ 1 RTT (на тест-сервере кратно меньше прежних ~510 мс) _(живой E2E на сервере: «от 00:00 до подачи: +1 мс», вместо ~510 мс на readMarketState+getRegFields ранее)_
- [x] Поведение при «дата ещё не открылась» сохранено (долбёжка) _(check-speed.js: без прогрева/долбёжка → полный путь читает календарь; пустой календарь → no_date)_

## Этап 13 — Telegram-тексты

**Цель**: все сообщения бота понятные, структурированные, со смайлами где уместно.

**Что делаем**:
- Переписать: /start, /status, итог подачи (успех/неуспех), алерты, старт/стоп.
- Единый стиль, без двусмысленностей (например, «заявка собрана» → ясная формулировка).

**Критерии приёмки** (закрытие → tag `stage-13-done`):
- [x] Все сообщения переписаны, читаются однозначно, структурно _(src/messages.js: единый стиль, минимум смайлов — только статус-точки 🟢/🔴/🟡, HTML-форматирование `<b>/<i>/<u>/<code>`; check-messages.js печатает все варианты)_
- [x] Клиент подтвердил, что тексты понятны (UAT по скриншотам) _(живые образцы отправлены в Telegram через send-samples.js; правки по фидбеку: убран жаргон аптайм/боевой/аккаунт/прогон, дата прописью с днём недели, дата подачи в строке каждого аккаунта, минимум смайлов)_

## Этап 14 — Финальная проверка с сайтом и надёжность

**Цель**: убедиться, что бот корректно работает с сайтом и устойчив к сбоям.

**Что делаем**:
- Боевой E2E (DRY_RUN=false) по доступному аккаунту, проверка брони в ЛК.
- Рестарт контейнера в момент работы, обработка ошибок сети, конфликт поллинга.

**Критерии приёмки** (закрытие → tag `stage-14-done`):
- [x] Боевая подача проходит, бронь видна в ЛК, уведомление приходит
- [x] Бот переживает рестарт, не теряет подписчиков/сессии, не дублирует подачу
- [x] Сетевые сбои и «нет даты» обрабатываются без падения

## Этап 15 — Готовность к боевому запуску (BY-сервер)

**Цель**: перенос на белорусский сервер и боевой режим (зависит от клиента).

**Что делаем**:
- BY-хостинг рядом с сетью сайта, перенос стека, оба аккаунта в `.env`, `DRY_RUN=false`.
- Боевая ночь в подстраховке (бот + ручная подача), несколько ночей.

**Критерии приёмки** (закрытие → tag `stage-15-done`):
- [x] Стек поднят на белорусском сервере, пинг до сайта мал (≈2 мс, выстрел +0/+1 мс от 00:00)
- [x] Оба аккаунта (она + муж) работают (логин обоих, параллельная подача DRY; реальный путь 2 мест проверен на песочнице)
- [ ] Несколько боевых ночей подряд успешны → переход в автономный режим (первая — 23.06 00:00)

## Этап 16 — Роли подписчиков и мониторинг сервера

**Цель**: разделить получателей уведомлений по ролям (жена / муж / разработчик) и
получать сигнал о недоступности сервера.

**Что делаем**:
- Подписка закрыта кодовым словом (`/start` просит слово; слово → роль). Env-чаты = dev.
- Маршрутизация: итог брони и алерты «подайте вручную» → всем; запуск/остановка и
  состояние сервера → только разработчику.
- Мониторинг: pre-flight «жив, готов к 00:00» разработчику при прогреве. (healthchecks.io
  убран — у клиента не работает регистрация; внешний watcher — опционально позже на
  GitHub Actions.)

**Критерии приёмки** (закрытие → tag `stage-16-done`):
- [ ] Кодовое слово подписывает с нужной ролью; неверное — отклоняется _(локально: check-roles.js 14/14; вживую — три роли получают свои сообщения)_
- [ ] Жена/муж получают итог и алерты, НЕ получают состояние сервера; разработчик получает всё
- [ ] Pre-flight приходит разработчику при ночном прогреве

## Этап 17 — Разнос второй заявки по времени (маскировка)

**Цель**: убрать «мгновенный дубль» в утренних списках админов рынка. В 00:00 с каждого
аккаунта уходит **только одна** заявка (выигрывает гонку), вторая — позже, с разрывом, чтобы
коллеги по списку не догадались про бота.

**Контекст**: лимит сайта — 2 места/сутки на аккаунт; сейчас `BOOKINGS_PER_ACCOUNT=2` подаёт
обе заявки подряд (~1–2 мс). Публичных списков на сайте нет — их видят только админы рынка,
читая день в день утром; две заявки одного имени с одинаковым штампом времени = палево.

**Что делаем**:
- В `runner.js` (`submitBookings`): 1-я заявка уходит сразу (00:00, гонка не страдает), каждая
  следующая — после паузы. Пауза **рандомизированная** (не фиксированные 2.000 с — фиксированный
  разрыв сам по себе след), независимо по аккаунтам, чтобы вторые заявки двух имён не совпадали.
- Конфиг: `SUBMIT_GAP_MIN_MS` / `SUBMIT_GAP_MAX_MS` (env). Предложение по умолчанию: **2000–8000 мс**
  (клиентка просила ~2 с — это нижняя граница; легко перенастроить вплоть до минут, если захочет
  больший разрыв в списке).
- 1-я заявка и метрика тайминга (`submitMs`, drift) не меняются; разрыв логируется по каждому аккаунту.
- При `BOOKINGS_PER_ACCOUNT=1` поведение не меняется (паузы нет — заявка одна).

**Риск (озвучить клиенту)**: в окно разрыва конкурент может перехватить 2-ю ячейку (особенно в
сезон). 1-е (основное) место это не затрагивает — оно берётся в 00:00. Клиентка осознанно
выбирает маскировку важнее гарантии 2-го места; разрыв конфигурируем.

**Критерии приёмки** (закрытие → tag `stage-17-done`):
- [ ] В 00:00 с каждого аккаунта уходит РОВНО одна заявка; вторая — позже, с разрывом
- [ ] Разрыв рандомизирован в диапазоне env, независим по аккаунтам (нет одинакового фикс. следа)
- [ ] 1-я заявка по-прежнему за ~+1 мс от 00:00 (гонка не пострадала); итог и тайминг в Telegram корректны
- [ ] Живой UAT (боевая ночь): в ЛК по 2 места на аккаунт; разрыв 2-й заявки виден по логам/времени
