# STATUS — текущее состояние проекта

> **Для Claude и человека**: это первый файл, который читается в новой сессии. После
> прочтения **обязательно** сверить с реальностью: `git tag -l "stage-*"`,
> `git log --oneline -20` — таблица ниже может быть устаревшей.

**Последнее обновление**: 2026-06-26 (ночь 26.06: найден и исправлен баг повторной подачи из-за лага ЛК — теперь приём сервером = успех, без дублей)
**Текущий этап**: 🚧 Этап 15 (боевые ночи) + 🚧 Этап 17 (фикс-2с) + 🚧 Этап 18 (персональный тайминг). Код+фикс готовы, локальные UAT зелёные.
**Следующий шаг**: живой UAT ночи **27.06 00:00** — обе заявки каждого аккаунта уходят чисто (1-я ~+1 мс, 2-я ~+2 с), БЕЗ долбёжки/дублей; в ЛК ровно 2 места/аккаунт; тайминг жене/мужу/dev корректен. После этого — теги `stage-17-done`/`stage-18-done`. Параллельно: этап 15, `stage-16-done`.

> 🐞 **Баг ночи 26.06 (исправлен):** у Петрусевича обе заявки сервер принял (code=201), но
> послеподачное чтение ЛК (страница с 50+ бронями) **бросило транзиентное исключение**, которое
> `runForContext` поймал **молча** → ложный «неуспех» → долбёжка **повторно отправила ещё 2 заявки**
> (6× code=201, тайминг поехал +5,5 / +8,2 с). У жены то же чтение прошло — кому «не повезёт» с
> транзиентом, дело случая. Усугубляло: долбёжка входила в быстрый путь (`attempt===1`) без защиты
> `isDone`. **Фикс (коммиты 244aaa3, 7925712):** приём сервером (code=201) = успех; чтение ЛК
> необязательно (try/catch — лаг ИЛИ ошибка не отменяют успех и не плодят дубли) + одна перечитка;
> долбёжка стартует с `attempt=2` (полный путь с `isDone`); сбой первой попытки теперь логируется.
> UAT `check-verify-nodup.js` 4/4 (вкл. сценарий «чтение ЛК бросает исключение»).
> ⚠ Проверить в ЛК Петрусевича на 03.07: должно быть ровно 2 места (сайт, похоже, отсёк дубли —
> подтверждено «2 из 2», но стоит глянуть глазами).

> ✅ **Этап 17 проверен вживую 25.06**: 4× code=201, 2/2 в ЛК на 02.07, разрыв виден
> (рандом 3,5 с и 5,6 с). **По решению клиентки разрыв переведён на ФИКСИРОВАННЫЙ — строго 2 с**
> (обе вторые заявки ~на +2 с; компромисс по «одинаковому штампу» принят осознанно).
> ⚠ Фикс-2с и этап 18 (персональный тайминг) задеплоены 25.06 вечером — **в реале ещё не прошли**,
> первая ночь — **26.06 00:00**. При следующем контакте спросить клиента, как прошло.

> ⚠ **ОПЕРАЦИОННОЕ (передать клиентке):** бот теперь берёт ОБЕ ячейки (2/2) на каждом аккаунте. В боевые ночи клиентке/мужу **НЕ нужно бронировать вручную** — иначе упрётесь в лимит и часть заявок бота отклонится. Если хочется подстраховки — сначала дождаться итога бота в Telegram, и только если бот не добрал — добирать вручную.

## ✅ Первая боевая ночь 23.06 00:00 — УСПЕХ (живой мониторинг с клиентом)

- **Точность выстрела +0 мс** от 00:00:00.000. Прогрев в 23:58 (оба аккаунта вошли,
  поля+тип=2, прогноз даты 2026-06-30).
- Подача: Стемпковская А.В. (`4131195c017pb4`) **+1 мс**, Петрусевич В.Г.
  (`3080391c002pb8`) **+5 мс**, по 2 запроса `create_zajav` каждый.
- **4× `code=201`** → **✅ 2 из 2 мест на 30.06 подтверждено в ЛК на ОБОИХ аккаунтах**
  (Комаровский). Долбёжки не было — успех с первого выстрела.
- **Этап 16 верифицирован вживую:** итог ушёл `→ 3 получателям` (жена+муж+dev подписались
  по кодовым словам), отдельно `→ 1 [dev]: ⏱ Тайминг подачи`. Pre-flight «🟢 готов к подаче»
  пришёл в 23:58.
- Следующая подача: **среда 24.06 00:00**, дата брони 2026-07-01.

> Этап 15 остаётся 🚧: нужно ещё несколько успешных ночей подряд для закрытия. Этап 16 —
> все 3 критерия приёмки выполнены вживую (готов к тегу `stage-16-done` после слова клиента).

> Как мониторить следующую ночь (для будущей сессии): подключиться к ~23:57 и стримить
> `ssh -i .secrets/deploy_key root@155.212.244.79 "cd /opt/bron-bot && docker compose logs app -f --since 2m"`.
> Красные флаги: `code=500/501`, `no_date`, серия сетевых ошибок → алерт о блокировке IP.

---

## Сводка этапов

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| #  | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|----|------|--------|-----------------|------------------|---------------|
| 0  | Каркас репозитория | ✅ | `stage-0-done` | `cff8c7c` | 2026-06-19 |
| 1  | Авторизация на сайте | ✅ | `stage-1-done` | `0f27284` | 2026-06-19 |
| 2  | Чтение состояния рынка | ✅ | `stage-2-done` | `a2f0f7b` | 2026-06-19 |
| 3  | Формирование заявки (dry-run) | ✅ | `stage-3-done` | `192fade` | 2026-06-19 |
| 4  | Верификация через ЛК | ✅ | `stage-4-done` | `618023c` | 2026-06-19 |
| 5  | Точный планировщик и тайминг | ✅ | `stage-5-done` | `0446aae` | 2026-06-19 |
| 6  | Мультиаккаунт | ✅ | `stage-6-done` | `949123c` | 2026-06-19 |
| 7  | Telegram-уведомления | ✅ | `stage-7-done` | `6963475` | 2026-06-19 |
| 8  | Хранилище и история | ✅ | `stage-8-done` | `8e403cd` | 2026-06-19 |
| 9  | Docker-деплой + CI/CD | ✅ | `stage-9-done` | `384e892` | 2026-06-19 |
| 10 | E2E тест-прогон на сервере | ✅ | `stage-10-done` | `d0029a3` | 2026-06-19 |
| 11 | Часовой пояс и механизм запуска | ✅ | `stage-11-done` | `c7aa858` | 2026-06-20 |
| 12 | Оптимизация скорости подачи | ✅ | `stage-12-done` | `c7dc398` | 2026-06-20 |
| 13 | Telegram-тексты | ✅ | `stage-13-done` | `b0bd87d` | 2026-06-20 |
| 14 | Финальная проверка с сайтом и надёжность | ✅ | `stage-14-done` | `c4d4758` | 2026-06-21 |
| 15 | Готовность к боевому запуску (BY-сервер) | 🚧 | `stage-15-done` | — | — |
| 16 | Роли подписчиков и мониторинг сервера | 🚧 | `stage-16-done` | — | — |
| 17 | Разнос второй заявки по времени (маскировка) | 🚧 | `stage-17-done` | — | — |
| 18 | Персональный тайминг жене/мужу + 2-я заявка dev | 🚧 | `stage-18-done` | — | — |

Детальные критерии приёмки каждого этапа — в [`ROADMAP.md`](ROADMAP.md).

## Этап 16 — Роли подписчиков и мониторинг (ЗАДЕПЛОЕН в master)

- Реализовано, **смержено в `master` и задеплоено на BY** (контейнер пересоздан, бот
  поднялся в боевом режиме): 3 роли (wife/husband/dev), подписка по кодовому слову
  (`/start` → слово → роль), ролевая рассылка (итог брони + алерты → всем; запуск/остановка
  + состояние сервера → dev), pre-flight «жив, готов к 00:00» разработчику при прогреве.
  Файлы: `config.js`, `notify.js`, `messages.js`, `orchestrator.js`, `index.js`,
  `.env.example`, `src/scripts/check-roles.js`.
- **healthchecks.io убран** (у клиента не работает регистрация): внешний dead-man's switch
  снят, `src/health.js` удалён. Остаётся pre-flight внутри бота. Возможная замена позже —
  watcher на GitHub Actions (без сторонней регистрации).
- **Отчёт тайминга разработчику** (`36388ec`): после ночной подачи dev отдельным сообщением
  получает точность выстрела (drift) + «от 00:00 до подачи» по каждому аккаунту (`submitMs`,
  уже измерялось в `runner.js`, теперь в результате и через `notifyDev`/`timingNotice`).
- UAT: `node src/scripts/check-roles.js` — 14/14 ✅; ролевая фильтрация проверена на живом
  `createNotifier`. На сервере подтверждено: старт чистый, Redis PONG, стартовое ушло
  `→ 1 получателям [dev]`, контейнер видит `TELEGRAM_CODE_WORDS`.
- **Кодовые слова (переданы клиенту):** `bron-zhena-7421` (Жена), `bron-muzh-3068` (Муж),
  `bron-dev-9153` (Разработчик). Уже прописаны в серверном `.env` на BY.
- ⚠ Жена и муж должны один раз отправить боту своё слово, иначе итог брони к ним не
  придёт. Желательно до ночи 23.06. Dev (env `2042819654`) получает всё без действий.

## Активная работа

**Этап 17 — разнос второй заявки по времени (маскировка). РЕАЛИЗОВАН, задеплоен в master.**
- **Запрос клиентки:** чтобы коллеги по утренним спискам админов не догадались про бота, в 00:00
  с каждого имени должна уходить только ОДНА заявка; вторая — с разрывом (она просила ~2 с).
- **Сделано:** в `submitBookings` (`runner.js`) 1-я заявка уходит в 00:00 (гонка не страдает), каждая
  следующая — после **рандомизированной** паузы `maskGapMs()` из env `SUBMIT_GAP_MIN_MS`/`SUBMIT_GAP_MAX_MS`
  (дефолт **2000–8000 мс**). Пауза внутри подачи аккаунта, а `attemptAll` крутит аккаунты параллельно —
  разрывы двух имён независимы и не совпадают. `submitMs`/drift считаются до паузы (1-я заявка не страдает).
  При `BOOKINGS_PER_ACCOUNT=1` паузы нет. Конфиг в `config.timing.submitGap`, `.env.example` обновлён.
- **Локальный UAT:** `node src/scripts/check-mask.js` — 3/3 ✅ (2 заявки, 2-я через паузу в диапазоне;
  6 замеров различаются → рандом; при 1 месте разрыва нет). `check-speed.js` без регрессии (быстрый путь цел).
- **Риск (сказать клиенту):** в окно разрыва 2-ю ячейку может перехватить конкурент; 1-е (основное)
  место не страдает. Разрыв конфигурируем (вплоть до минут через env).
- **Осталось:** живой UAT в боевую ночь — 2 места на аккаунт в ЛК + видимый разрыв 2-й заявки по логам
  → tag `stage-17-done`.

**Параллельно:** этап 15 — наблюдать боевые ночи; этап 16 — готов к `stage-16-done` по слову клиента.

**BY-сервер (боевой, для этапа 15)**: `155.212.244.79` (root), Минск, Yedinyye Resheniya LLC. Пинг до сайта `87.252.228.216` — min/avg/max 1.3/2.1/6.0 мс, 0% потерь. SSH host-key: `SHA256:I3kmpZuP/2Y8fNANYEZ3KH+Wn/2Mot2xK1YRzqaMAAo`. Пароль — в `.secrets`/доступах, не в репо.

## Прод (BY-сервер, БОЕВОЙ — cutover выполнен на этапе 15)

- Сервер: `155.212.244.79` (Ubuntu 22.04, 1 CPU / 1.9GB + swap 1.1G), Минск, каталог `/opt/bron-bot`. Docker 29.6, Compose v5.1. SSH host-key `SHA256:I3kmpZuP/2Y8fNANYEZ3KH+Wn/2Mot2xK1YRzqaMAAo`. Deploy-ключ `.secrets/deploy_key` (тот же, что был у тест-сервера).
- **CI/CD репоинтнут на BY**: GitHub Secret `SERVER_HOST` = `155.212.244.79` (поменял клиент). Push в `master` → автодеплой на BY (проверено: коммиты доезжают, app пересобирается).
- **Боевой режим**: `DRY_RUN=false`, `BOOKINGS_PER_ACCOUNT=2` (бот берёт обе ячейки на дату), оба аккаунта в `.env`. `TELEGRAM_BOT_TOKEN` — рабочий токен (перенесён с тест-сервера), `TELEGRAM_CHAT_IDS=2042819654` (env-фолбэк подписчика). Серверный `.env` — `.secrets/server-by.env` (gitignored).
- Замер с BY (2-акк E2E): выстрел `create_zajav` за **+0/+1 мс от 00:00** (1 RTT). Реальный путь 2 мест проверен на песочнице (2× code 201 → 2 брони в ЛК → сняты). Планировщик: «вт 23.06 00:00» → дата брони 30.06, понедельник пропущен.
- **Первая боевая ночь: 23.06 00:00** (ночь на пн 22.06 пропускается). Бот реально займёт по 2 места на 30.06 на каждом аккаунте и пришлёт итог в Telegram.

## Тест-сервер — ВЫКЛЮЧЕН (cutover завершён)

- `163.5.153.147`: `docker compose down`, `TELEGRAM_BOT_TOKEN` стёрт из `.env`. Стек погашен. Можно удалять VM (по решению клиента). Подписчик и историю боевой ведёт BY.

## Репозиторий / CI

- Репо: https://github.com/dimatolpygin/torgi — `master` (автодеплой на BY). По решению клиента разработка тоже идёт в `master`.
- CI/CD: push в `master` → GitHub Actions (`appleboy/ssh-action`) → `git reset --hard` + `docker compose up -d --build`. Secrets: SERVER_HOST(=BY), SERVER_USER, SSH_PRIVATE_KEY. `.env` на сервере автодеплой НЕ трогает (gitignored, живёт на сервере).

## Известные блокеры

**Боевые ночи ещё не отнаблюдали.** Cutover на BY выполнен, `DRY_RUN=false`, всё проверено
DRY/песочницей, но реальную гонку в 00:00 в сезон (и подтверждение 2/2 в ЛК на боевых
аккаунтах) увидим только в первую боевую ночь **23.06 00:00** и далее. До этого этап 15 — 🚧.
Технических блокеров нет.

## История закрытий

2026-06-21 — Этап 14: Финальная проверка с сайтом и надёжность — tag stage-14-done — все 3 критерия проверены вживую. **Пункт 1 (боевая подача):** реальный `create_zajav` с `DRY_RUN=false` на тестовом аккаунте (`1121298c017rb8`) → `code 201` «Ваша заявка принята» за 291 мс (холодный сокет), бронь `2026-06-28` Комаровский/Торговый ряд/овощи (key=88033) подтверждена отдельным запросом к ЛК; тестовая бронь сразу снята (реверс AJAX `POST account/ {ID_USER,ACTION:del,LIST}` из rinki.gupr.js, `code 200`), ЛК вернулся к 51 брони — тест полностью обратим. Уведомление — путь оркестратора (notifyRunResult), доказан в этапе 7 + живая доставка стартовых/стоповых сообщений подписчику. Скрипты: `check-live-submit.js`, `delete-booking.js`. **Пункт 2 (рестарт/дедуп):** на тест-сервере `docker compose restart app` (и сам автодеплой) — грейсфул SIGTERM со стоп-уведомлением, подъём за ~2с, переподключение PG/Redis/Telegram; подписчик `2042819654` и `last_run` ({«28 июня — успех»}) пережили рестарт (Redis), планировщик пересчитал «Следующая подача: вт 23.06» с пропуском понедельника 22.06, дубль-подача НЕ запустилась. **Пункт 3 (сетевые сбои/«нет даты»):** детерминированный UAT `check-resilience.js` (фейк-клиент) — no_date→reason no_date, сетевой сбой→ловится оркестраторным catch→reason error (процесс жив), не залогинен→not_logged_in; 3/3 локально и в контейнере на сервере. Решение: `isDone`-guard в быстрый путь (00:00) НЕ добавляли — лишний Redis-запрос замедлил бы выстрел ради практически невозможного сценария (после 00:00 планировщик берёт следующую ночь); дедуп остаётся в долбёжке. Разработка по решению клиента ведётся в `master` (push → автодеплой).

2026-06-20 — Этап 13: Telegram-тексты — tag stage-13-done — все тексты вынесены в `src/messages.js` (чистые билдеры), подключены в notify/index/orchestrator. Включён `parse_mode: 'HTML'`, подставляемые значения экранируются (`esc`). По фидбеку клиента: (1) убран жаргон — аптайм→«бот работает … без перерыва», боевой→рабочий, аккаунт→кабинет, прогон→подача; (2) дата прописью с днём недели («воскресенье, 21 июня 2026») вместо ISO; (3) дата подачи в строке каждого аккаунта («место забронировано на 23 июня»), дата всегда из предвычисления (+7); (4) минимум смайлов — только статус-точки 🟢/🔴/🟡 и одна ⚠ в алертах, остальное HTML (<b>/<i>/<u>/<code>). Проверка: `check-messages.js` (печать всех вариантов без отправки), живые образцы в Telegram через `send-samples.js` (без long polling — без конфликта getUpdates). Развёрнуто на тест-сервере (`b0bd87d`), образцы доставлены подписчику.

2026-06-20 — Этап 12: Оптимизация скорости подачи — tag stage-12-done — в 00:00 уходит единственный запрос `create_zajav`. Прогрев (`warmupAccount` в `runner.js`) заранее тянет поля формы (`getRegFields`) и тип места (`getTypeMest`); дата брони предвычисляется в оркестраторе = ночь подачи + `bookingLeadDays`(7). `attemptForAccount` получил быстрый путь (attempt=1 с прогревом → собрать payload локально + только `submitOrder`, замер «от 00:00 до подачи» через `ctx.targetMs`) и полный путь (нет прогрева/долбёжка → readMarketState + getRegFields, сохраняет «дата не открылась» и коррекцию по календарю). Общий `handleSubmit` (dry-run/отклонение/верификация). Локально `check-speed.js` (3/3): быстрый путь — 0 предварительных чтений; без прогрева — читает календарь; пустой календарь → no_date. Живой dry-run E2E на тест-сервере (деплой `c7dc398`): прогрев в −25с (поля+тип=2, прогноз 2026-06-27), в 00:00 «от 00:00 до подачи: +1 мс (1 запрос create_zajav)» против прежних ~510 мс на чтениях — кратно меньше, ≈1 RTT. Конфиг: `BOOKING_LEAD_DAYS` (env, дефолт 7).

2026-06-20 — Этап 11: Часовой пояс и механизм запуска — tag stage-11-done — найден реальный баг: логи шли в UTC, т.к. pino-pretty без префикса `SYS:` форматирует время в UTC и `TZ=Europe/Minsk` контейнера на это не влияет (правка TZ до паузы была косметической). Фикс `src/logger.js`: `translateTime: 'SYS:dd.mm.yyyy HH:MM:ss'` — вместе с TZ=Europe/Minsk даёт минское время (доказано: без SYS=05:59 UTC, с SYS=08:59 Минск). Новый `src/scripts/check-schedule.js`: ближайшие 10 ночей подачи → дата брони (+7), проверка отсутствия понедельника в ночах и датах брони, детерминизм пересчёта. Живой UAT на тест-сервере (деплой `c7aa858`): в контейнере `TZ=Europe/Minsk`, лог pino `[08:59]` против docker-метки `05:59Z` — минское время ✅; `docker compose restart app` → контейнер поднялся, планировщик пересчитал «Следующая подача: вс 21.06 00:00» от текущего времени (внутренний luxon-цикл, не крон, состояние не теряется) ✅. Открытый вопрос «TZ оставить/откатить» решён: оставляем TZ + SYS:.

2026-06-19 — Этап 10: E2E тест-прогон на сервере — tag stage-10-done — на сервере полный цикл с подставной целевой минутой (`src/scripts/check-e2e.js`, опция `targetMs` в orchestrator). В ходе E2E найден и исправлен критический баг парсинга календаря: бот искал `<a class="day">`, а реальная разметка — `<td class="coolday ic_day">N</td>` (кликабельность на `.ic_day` по rinki.reg.js), из-за чего всегда видел «дат нет» и в бою не подал бы заявку. После фикса E2E прошёл успешный путь: сессия восстановлена из Redis → состояние рынка (день 26, 6/2026) → payload собран (arr_date={0:"26"}, ARR_ASSORT={0:"2"}, is_login=1) → dry-run «заявка собрана на 2026-06-26» → уведомление в Telegram доставлено → запись в Postgres (success=true, dry_run=true, drift=+0мс). Ручная бронь клиента на 26.06 видна боту (52 брони в ЛК). Успешная боевая подача (DRY_RUN=false) — пост-этап на белорусском сервере.

2026-06-19 — Этап 9: Docker-деплой + CI/CD — tag stage-9-done — стек развёрнут на тест-сервере `163.5.153.147:/opt/bron-bot` через okdeploy (swap 2G, Docker 29.6, клон master). `.github/workflows/deploy.yml` (appleboy/ssh-action), `scripts/install.sh`, `.gitattributes` (LF). Секреты в GitHub: SERVER_HOST/SERVER_USER/SSH_PRIVATE_KEY (deploy-ключ ed25519 в `.secrets/`). Проверено вживую: первый прогон Actions упал `dial tcp ***:22 i/o timeout` (опечатка в SERVER_HOST), после исправления пуш в master (`384e892`) → Actions выкатил на сервер, HEAD обновился, в логах `v0.2.0`, бот в long polling, планировщик активен (след. подача 20.06 00:00 Minsk). `restart: unless-stopped`. Порты PG/Redis наружу не пробрасываются.

2026-06-19 — Этап 8: Хранилище и история — tag stage-8-done — схема Postgres `src/schema.sql` (accounts, attempts), `ensureSchema/recordAttempt/getRecentAttempts` в `db.js`; оркестратор пишет каждую попытку с drift и деталями. Redis-сессии `src/session.js`: куки (TTL 12ч) + флаг «сегодня подано» (TTL 3 сут). `prepareAccount` восстанавливает сессию из Redis без лишнего логина; `attemptForAccount` ставит флаг после успеха и пропускает повтор в долбёжке. ФИО при восстановлении дочитывается со страницы reg. Полный live-UAT (`docker compose run --rm app node src/scripts/check-storage.js`): запись в PG читается обратно (✅), флаг в Redis (✅), первый prepare реально вошёл и сохранил куку, второй восстановил сессию из Redis без логина — «Иванов Александр Эдуардович», restored=true (✅). Порты PG/Redis наружу не пробрасываются — UAT в сети compose.

2026-06-19 — Этап 7: Telegram-уведомления — tag stage-7-done — бот на telegraf (`src/notify.js`): `/start` регистрирует chat_id (подписчики в Redis с фолбэком в память + env `TELEGRAM_CHAT_IDS`), `/status` отдаёт состояние сервера (аптайм, след. подача, аккаунты, режим, последний прогон). После прогона — итог по каждому аккаунту с «подайте вручную» при неуспехе. Алерт о возможной блокировке IP (серия ошибок на подаче). Оркестратор ночи `src/orchestrator.js`, всё связано в `index.js` (стартовое/стоп-уведомление, планировщик). Тексты без эмодзи. UAT (`node src/scripts/check-telegram.js`): в Telegram дошли приветствие, итог 1 успех + 1 «НЕ ПОДАНО», алерт и `/status`. Redis локально недоступен — фолбэк в память сработал.

2026-06-19 — Этап 6: Мультиаккаунт — tag stage-6-done — параллельная подготовка и подача по аккаунтам с изоляцией сессий (свой SiteClient/кука на аккаунт), логи с префиксом [логин]. Проверено: при двух аккаунтах (верный + неверный пароль) первый вошёл, второй нет — сессии не путаются. Увеличен connect-timeout клиента (30с) для дальнего сервера. UAT: `node src/scripts/check-multi.js`.

2026-06-19 — Этап 5: Точный планировщик и тайминг — tag stage-5-done — точность выстрела +0 мс (busy-wait на финише компенсирует неточность setTimeout). Пропуск понедельника: воскресенье 21.06 → следующая подача вторник 23.06. Режим долбёжки до успеха/окна. Доработка (по фидбеку о риске блокировки IP): адаптивный интервал, джиттер, потолок запросов/мин, тревога onPossibleBlock при серии сетевых ошибок — долбёжка безопасна для единственного IP. UAT: `node src/scripts/check-scheduler.js` (5 проверок).

2026-06-19 — Этап 4: Верификация через ЛК — tag stage-4-done — парсер таблицы #zajav в account/ распарсил 51 бронь тестового аккаунта (дата YYYY-MM-DD, рынок, тип места, ассортимент по чекбоксам f_ovosh и т.д.). Поиск по Комаровский+овощи находит бронь. verifyBooking подтверждает успех по факту в ЛК. UAT: `node src/scripts/check-bookings.js`.

2026-06-19 — Этап 3: Формирование заявки (dry-run) — tag stage-3-done — собран POST `create_zajav/` (rinok=10, type_mesta=2, ARR_ASSORT={0:"2"} овощи, is_login=1, f_sogl=1), виден в логе под DRY_RUN. Диагностика на живом сервере (запрос с пустой датой): `code 500, {arr_date:"no_date"}`, поля персоны без ошибки → залогиненному нужна только дата+ассортимент, заявка не создаётся. UAT: `node src/scripts/check-order.js` (+ PROBE=1 для диагностики).

2026-06-19 — Этап 2: Чтение состояния рынка — tag stage-2-done — на Комаровском (id=10): тип места по умолчанию «Торговый ряд» (value=2), лимит 2 брони/день (совпало со словами клиента), днём «свободных дат нет» — корректное чтение пустого состояния. Логика выяснена из rinki.reg.js; найден реальный эндпоинт подачи `create_zajav/`. UAT: `node src/scripts/check-market.js`.

2026-06-19 — Этап 1: Авторизация на сайте — tag stage-1-done — на живом сайте под тестовым аккаунтом клиента: вход успешен, в логе «Вошли как Иванов Александр Эдуардович», кука gorodid получена и переиспользуется. Неверный пароль корректно отклонён. UAT через `node src/scripts/check-auth.js`.

2026-06-19 — Этап 0: Каркас репозитория — tag stage-0-done — `docker compose up` поднял app+postgres+redis, в логе подтверждено подключение к Postgres 16.14 и Redis (PONG), сообщения на русском с датой/временем. Секреты вынесены в `.env` (в репо только `.env.example`).

## Протокол обновления

**При старте сессии**:
1. Прочитать этот файл.
2. `git tag -l "stage-*"` и сверить с колонкой «Закрывающий тег».
3. `git log --oneline -10` — что менялось последним.
4. Только после этого приступать к работе.

**При взятии этапа в работу**:
1. Обновить «Текущий этап» и «Активная работа».
2. Статус 🚧 в таблице.
3. Закоммитить отдельно: `chore(status): start stage N`.

**При закрытии этапа**:
1. Все чекбоксы в `ROADMAP.md` отмечены `[x]` и реально проверены.
2. Тег: `git tag stage-N-done -m "<описание>"`.
3. В таблице: ✅, тег, последний коммит, дата.
4. Запись в «Историю закрытий».
5. Новый «Текущий этап» и «Следующий шаг».
6. Закоммитить: `chore(status): close stage N`.

**Если этап заблокирован**:
1. Статус ⏸ в таблице.
2. В «Известные блокеры»: что блокирует, кого ждём, что нужно для разблокировки.

## Что НЕ хранится здесь

- Длинные размышления и планы — это `ROADMAP.md`.
- TODO внутри текущего этапа — таск-лист активной сессии.
- История изменений кода — `git log`.

Этот файл — **только снимок текущего состояния по этапам**.
